---
import Layout from "@layouts/Layout.astro";
import NodeRenderer from "@nexus-archive/ui/components/NodeRenderer.tsx";

const { repo, node } = Astro.params;
---

<Layout>
  <NodeRenderer repo={repo as string} node={node as string} />
</Layout>
