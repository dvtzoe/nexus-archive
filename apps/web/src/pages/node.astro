---
import Layout from "@web/layouts/Layout.astro";
import NodeRenderer from "@ui/components/NodeRenderer";

const { url } = Astro;
const repo = url.searchParams.get("repo") || "";
const node = url.searchParams.get("node") || "";
---

<Layout>
  <NodeRenderer repo={repo} node={node} />
</Layout>
