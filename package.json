{
  "name": "nexus-archive",
  "version": "0.1.0",
  "description": "A publishing platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "pnpm -rF @nexus-archive/web run dev",
    "build": "turbo build",
    "build:web": "pnpm -rF @nexus-archive/web run build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean",
    "start:repo": "pnpm -rF @nexus-archive/repository run start"
  },
  "packageManager": "pnpm@10.15.1",
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  }
}
